ns NSharp.Tests

use System

public struct MyStruct{M}
    fn x M
    fn y []?str
    fn z ?[]str

del Test{C}(x C)

interface ITest{D}
    fn Tester{X}(z D)

class MyClass{T, V}
    fn new() is ; empty
    fn Test{W}(x W)
        x ?
        {
            2 is true,
            3 is false
        }
