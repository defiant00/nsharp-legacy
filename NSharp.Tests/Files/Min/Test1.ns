ns NSharp.Tests

use System

public struct MyStruct{M}
    fn x M
    fn y []?str
    fn z ?[]str

del Test{C}(x C)

interface ITest{D}
    fn Tester{X}(z D)

class MyClass{T, V}
    var delims []char = [' ', '\t']
    var tests []char = []